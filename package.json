{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:production": "pnpm --filter @workspace/fintechpresshub run build && pnpm --filter @workspace/api-server run build",
    "start": "node --enable-source-maps artifacts/api-server/dist/index.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "seed:auto": "pnpm --filter @workspace/scripts run seed:auto",
    "seed:reset": "pnpm --filter @workspace/scripts run seed:reset --",
    "setup:check": "pnpm --filter @workspace/scripts run setup:check",
    "bootstrap": "bash scripts/bootstrap.sh",
    "setup": "bash scripts/setup.sh"
  },
  "private": true,
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.3"
  }
}
